# Discord
discord.py==2.4.0

# Memory
mem0ai[nlp]==0.1.101       # [nlp] enables BM25 hybrid search inside Mem0
qdrant-client>=1.12.1

# Hybrid search (BM25 sparse vectors)
# fastembed removed as it was unused and caused slow boot times

# LLM — Google Gemini
google-genai>=1.0.0
google-generativeai==0.8.3    # Legacy SDK — pinned for mem0ai 0.1.101 LLM provider

# Local embeddings (torch installed separately in Dockerfile for CPU-only)
sentence-transformers>=2.7.0

# Data validation
pydantic==2.8.2
pydantic-settings==2.3.0

# NLP
spacy==3.7.4

# Media ingestion
httpx==0.27.0
readability-lxml==0.8.1
Pillow==10.4.0

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0
PyYAML==6.0.2
python-frontmatter==1.1.0

# Logging
structlog==24.4.0
